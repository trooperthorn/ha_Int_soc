# Test harness pins for .github/workflows/test.yml's pytest job.
# pytest-homeassistant-custom-component pins the Home Assistant core version
# this suite runs against; home-assistant-frontend must match the pin in
# that core's package_constraints.txt (bump both together, work plan
# section 7). Kept as its own file (rather than inline in the workflow) so
# Dependabot's pip ecosystem can open update PRs for it.
pytest-homeassistant-custom-component==0.13.316
home-assistant-frontend==20260826.1
aiousbwatcher
pyserial
pyudev
