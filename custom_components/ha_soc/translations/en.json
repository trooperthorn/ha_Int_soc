{
  "config": {
    "step": {
      "user": {
        "title": "Set up HA SOC",
        "description": "HA SOC centralizes user access, audit logging, dashboard permissions, device/integration vulnerability tracking, and a SOC-style risk dashboard. Only one instance is needed per Home Assistant install."
      }
    },
    "abort": {
      "single_instance_allowed": "HA SOC is already configured. Only a single instance is supported."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HA SOC settings",
        "description": "All HA SOC settings live in the panel's Settings tab, which is available to the account owner only. {where}"
      }
    }
  },
  "entity": {
    "sensor": {
      "posture_score": { "name": "Security posture score" },
      "open_detections": { "name": "Open detections" },
      "users_at_risk": { "name": "Users at risk" },
      "user_risk": { "name": "Risk" }
    },
    "binary_sensor": {
      "suspicious_activity": { "name": "Suspicious activity" }
    }
  },
  "issues": {
    "admin_without_mfa": {
      "title": "Admin account without multi-factor authentication",
      "description": "The admin account \"{user_name}\" has no MFA module enrolled. An admin credential with no second factor is a full-takeover risk from a single leaked password. Enroll MFA for this user in Settings > People, or acknowledge this risk in the HA SOC Users view."
    },
    "trusted_networks_permissive": {
      "title": "Trusted Networks auth provider is permissively configured",
      "description": "The trusted_networks auth provider is configured with a network range of {network}. This bypasses password authentication (and MFA, which trusted_networks never applies) for anyone on that range. Review the `trusted_networks` block in configuration.yaml."
    },
    "http_insecure": {
      "title": "Home Assistant is reachable without TLS",
      "description": "external_url is set to an http:// address, or use_ssl is disabled with no reverse proxy detected. Credentials and session tokens would transit in cleartext. See the HA SOC misconfiguration report for detail."
    },
    "ip_ban_disabled": {
      "title": "Automatic IP banning is disabled",
      "description": "`ip_ban_enabled` is false (or `login_attempts_threshold` is unset), so repeated failed logins never result in an automatic ban. Consider enabling ip_ban_enabled and setting a finite login_attempts_threshold in the http: section of configuration.yaml."
    },
    "scanner_finding": {
      "title": "Security scanner finding in {domain}",
      "description": "HA SOC's integration scanner flagged {count} potential issue(s) in the \"{domain}\" integration's source. This is a heuristic finding — review it in the HA SOC Scanner view before taking any action against the integration or its author."
    },
    "device_vulnerability": {
      "title": "Possible known vulnerability on {device_name}",
      "description": "HA SOC matched \"{device_name}\" against {count} candidate CVE(s). This is a heuristic match — confirm or dismiss it in the HA SOC Vulnerabilities view."
    },
    "misconfig_finding": {
      "title": "{title}",
      "description": "{summary} Review this finding in the HA SOC misconfiguration report."
    },
    "stale_access_token": {
      "title": "Long-lived access token unused for a long time",
      "description": "The token \"{token_name}\" belonging to \"{user_name}\" hasn't been used since {last_used}. A long-lived access token never expires on its own — an old, forgotten one is standing attack surface if it ever leaked. Revoke it from the HA SOC Users & Access tab if it's no longer needed."
    }
  }
}
